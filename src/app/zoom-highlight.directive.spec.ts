import { ZoomHighlightDirective } from './zoom-highlight.directive';

describe('ZoomHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new ZoomHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
